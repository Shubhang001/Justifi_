import 'package:flutter/material.dart';

class FloatCases extends StatelessWidget {
  const FloatCases({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
